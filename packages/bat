#!/usr/bin/env bash

# https://github.com/sharkdp/bat

sudo dra download -i -s "bat_{tag}_amd64.deb" sharkdp/bat

# dra download -i -s "bat-v{tag}-x86_64-unknown-linux-gnu.tar.gz" -o ~/.local/bin sharkdp/bat

# bat --completion bash > ~/.local/share/bash-completion/completions/bat
# bat --completion zsh > ~/.local/share/zsh-completion/_bat
